installer terraform au préalable
exécuter les commandes suivant pour que le projet se lance :

cd .\app\  
terraform.exe init 
terraform.exe apply

ouvrer le fichier IP_ec2.txt pour voir url, entrez la dans un navigateur (voir image)

pour tout supprimer entrze la commande :
terraform destroy  
